import { View, Text } from "react-native";
import React from "react";

const VideoCart = ({
  video: {
    title,
    thumbnail,
    video,
  }
}) => {
  return (
    <View>
      <Text>VideoCart</Text>
    </View>
  );
};

export default VideoCart;
